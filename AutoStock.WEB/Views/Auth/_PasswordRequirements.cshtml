<div class="password-requirements" data-password-requirements aria-live="polite">
    <span class="password-requirement" data-password-rule="length">En az 6 karakter</span>
    <span class="password-requirement" data-password-rule="upper">Bir büyük harf</span>
    <span class="password-requirement" data-password-rule="lower">Bir küçük harf</span>
    <span class="password-requirement" data-password-rule="digit">Bir rakam</span>
    <span class="password-requirement" data-password-rule="match">Şifreler eşleşiyor</span>
</div>

<style>
    .password-requirements {
        margin: -6px 0 16px;
        display: flex;
        flex-wrap: wrap;
        gap: 7px;
    }

    .password-requirement {
        min-height: 28px;
        padding: 5px 9px;
        border: 1px solid #e2e8f0;
        border-radius: 999px;
        background: #f8fafc;
        color: #64748b;
        font-size: 12px;
        font-weight: 700;
        transition: border-color .16s ease, background .16s ease, color .16s ease;
    }

    .password-requirement.is-valid {
        border-color: #a7f3d0;
        background: #ecfdf5;
        color: #047857;
    }

    .password-requirement.is-invalid {
        border-color: #fecaca;
        background: #fff7f7;
        color: #b91c1c;
    }
</style>
